const request = require('request');
const loggerMessages = require('./loggerMessages');

class TfLRequest {

    constructor(appID, appKey) {
        this.appID = appID;
        this.appKey = appKey;
    }

    function getData() {
        
    }

}
